while(start + wordLen * words.length <= s.length() && end + wordLen <= s.length())

substring(int beginIndex) (to the end)
Returns a new string that is a substring of this string.

Can't use Integer.ValueOf(char) to get a char's value, it can only get the ASCII value of that char
use Character.getNumericValue(char)